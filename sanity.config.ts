// Root Sanity config used by the Sanity CLI (`sanity dev`, `sanity deploy`).
// The actual configuration lives in src/sanity/config.ts (also imported by the
// embedded Studio route at /studio).
export { default } from "./src/sanity/config";
